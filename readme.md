# LibreChat с встроенным RAG

Персональная настройка LibreChat с поддержкой Retrieval-Augmented Generation (RAG) для работы с документацией и файлами.

## Возможности

- **LibreChat** - открытый интерфейс для работы с различными LLM
- **RAG (Retrieval-Augmented Generation)** - семантический поиск по загруженным документам
- **Локальные эмбеддинги** - через Ollama без внешних API
- **Векторная база данных** - PostgreSQL + PGVector
- **MCP файловый сервер** - прямой доступ к файлам в `/app/data`
- **Поддержка форматов** - PDF, DOCX, TXT, MD, HTML и другие

## Архитектура

```
LibreChat (UI) 
    ↓
┌─── RAG API (FastAPI) ────┐    ┌─── MCP filesystem ────┐
│   PostgreSQL + PGVector  │    │   Прямой доступ       │
│   Ollama (эмбеддинги)   │    │   к файлам в /data    │
└─────────────────────────┘    └───────────────────────┘
```

## Требования

- Docker и Docker Compose
- 8GB+ RAM (рекомендуется для Ollama)
- OpenAI API ключ, ClaudeAI ключ

## Установка

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd librechat-rag
```

### 2. Настройка переменных окружения
```bash
# Скопируйте шаблон
cp .env.example .env

# Отредактируйте .env и заполните:
# - OPENAI_API_KEY (обязательно)
# - CLAUDE_API_KEY (обязательно)
# - NEXTAUTH_SECRET, SESSION_SECRET, JWT_SECRET (случайные строки)
# - DOCS_PATH и DATA_PATH (пути к локальным папкам с документами)
# - При необходимости измените пароли для БД
```

**Генерация секретов:**
```bash
# Для генерации случайных секретов (32 символа)
openssl rand -base64 32
```

### 3. Создание директорий для документов
```bash
# Создайте локальные директории (или используйте существующие)
mkdir -p docs data

# Обновите пути в .env:
# LOCAL_DOCS_PATH=/path/to/your/docs
# LOCAL_DATA_PATH=/path/to/your/data
```

### 4. Запуск системы
```bash
# Запуск всех сервисов
docker-compose up -d

# Загрузка модели эмбеддингов (при первом запуске)
docker-compose exec ollama ollama pull nomic-embed-text

# Проверка статуса
docker-compose ps
```

### 5. Доступ к приложению
Откройте http://localhost:3080 в браузере

## Использование

### RAG (семантический поиск по документам)

**Загрузка документов:**
1. В интерфейсе LibreChat нажмите на скрепку (📎)
2. Выберите файлы или перетащите их в чат
3. Выберите "Upload for file search" 
4. Дождитесь завершения индексации

**Настройка поведения:**
- **Отключите "Resend Files"** - документы как дополнительный контекст
- **Включите "Resend Files"** - точный поиск по документам

### MCP файловый сервер

**Прямой доступ к файлам:**
1. Положите файлы в папку, указанную в `DATA_PATH`
2. В чате выберите dropdown "filesystem"
3. Команды: "прочитай файл X", "покажи содержимое папки"

### Рекомендуемый промпт для RAG
```
Ты - эксперт по [тематике] с доступом к документации. 
При ответах используй как свои знания, так и загруженные документы.
Всегда указывай источник: "Согласно документации..." vs "Из общих знаний..."
Объединяй информацию из документа со своими знаниями для полных ответов.
```

## Мониторинг и отладка

### Проверка статуса сервисов
```bash
# Статус всех контейнеров
docker-compose ps

# Логи конкретного сервиса
docker-compose logs librechat
docker-compose logs rag_api
docker-compose logs ollama

# Проверка векторной БД
docker-compose exec vectordb psql -U myuser -d mydatabase -c "SELECT COUNT(*) FROM langchain_pg_embedding;"
```

### Health проверки
```bash
# RAG API
curl http://localhost:8000/health

# Ollama
curl http://localhost:11434/api/tags

# LibreChat
curl http://localhost:3080/api/health
```

## Настройка

### Основные параметры RAG (в .env.local)
- `RAG_CHUNK_SIZE=800` - размер чанков документов
- `RAG_CHUNK_OVERLAP=100` - перекрытие чанков
- `RAG_USE_FULL_CONTEXT=false` - использовать весь документ vs поиск
- `RAG_DEBUG_API=false` - отладочная информация

### Модели и провайдеры
- **Эмбеддинги:** Ollama (nomic-embed-text) - локально
- **LLM:** OpenAI через API (можно добавить другие в librechat.yaml)

### Настройка путей
- `DOCS_PATH` - документы для общего доступа
- `DATA_PATH` - файлы для MCP файлового сервера

## Особенности использования

### Два способа работы с файлами:

**1. RAG (рекомендуется для документации):**
- Семантический поиск по содержанию
- Автоматическая индексация в векторную БД
- Лучше для больших документов и справочников

**2. MCP файловый доступ (для конкретных файлов):**
- Прямое чтение файлов
- Быстрый доступ без индексации
- Лучше для конфигурационных файлов и скриптов

## Ограничения

- Нет автоматической загрузки документации по URL
- PDF файлы могут долго обрабатываться при первой загрузке
- Требует достаточно RAM для работы с большими документами

## Структура файлов

```
.
├── docker-compose.yml     # Конфигурация Docker
├── librechat.yaml        # Настройки LibreChat + MCP
├── .env.example          # Шаблон переменных окружения
├── .env.local            # Реальные переменные (не в git)
├── docs/                 # Локальные документы (DOCS_PATH)
├── data/                 # MCP файловый доступ (DATA_PATH)
└── README.md            # Этот файл
```

## Устранение неполадок

### RAG не работает
1. Проверьте что модель nomic-embed-text загружена: `docker-compose exec ollama ollama list`
2. Проверьте логи RAG API: `docker-compose logs rag_api`
3. Убедитесь что файлы проиндексированы в vectordb

### MCP сервер не работает
1. Проверьте логи LibreChat: `docker-compose logs librechat | grep -i mcp`
2. Убедитесь что `MCP_ENABLED=true` в .env.local
3. Проверьте доступность файлов в `DATA_PATH`

### Медленная работа
1. Увеличьте ресурсы Docker
2. Уменьшите `RAG_CHUNK_SIZE` до 500-600
3. Используйте `RAG_USE_FULL_CONTEXT=false` для больших документов

### Проблемы с памятью
1. Ограничьте размер загружаемых файлов
2. Мониторьте использование RAM: `docker stats`
3. Перезапустите сервисы: `docker-compose restart`

## Развитие проекта

**После развертывания в Proxmox (x86_64):**
- MCP серверы с поддержкой загрузки по URL
- Альтернативные векторные БД (Qdrant, Chroma)
- GPU ускорение для Ollama
- Автоматическое обновление документации

**Текущая конфигурация** - стабильная основа для дальнейшего развития RAG возможностей.
